# 白屏问题诊断指南

## 已修复的问题

1. ✅ 移除了错误的 Ant Design 样式导入 (`antd/dist/reset.css`)
2. ✅ 添加了错误边界组件 (ErrorBoundary)
3. ✅ 添加了API调用的错误处理

## 诊断步骤

### 1. 检查浏览器控制台
按 `F12` 打开开发者工具，查看 Console 标签是否有红色错误信息。

常见错误可能包括：
- `Module not found`
- `Cannot read property ...`
- `Failed to fetch`
- 其他 JavaScript 运行时错误

### 2. 检查网络请求
在开发者工具的 Network 标签中，查看：
- `localhost:3000` 的请求是否成功（状态码 200）
- `/api/test-categories` 的请求是否成功
- 是否有任何请求失败（红色状态码）

### 3. 清除浏览器缓存并硬刷新
- 按 `Ctrl + Shift + R` (Windows) 或 `Cmd + Shift + R` (Mac) 硬刷新
- 或在开发者工具中右键刷新按钮，选择"清空缓存并硬性重新加载"

### 4. 检查服务器状态

**后端服务器（端口8000）:**
```powershell
Invoke-WebRequest http://localhost:8000/ -UseBasicParsing
```

**前端服务器（端口3000）:**
```powershell
Invoke-WebRequest http://localhost:3000/ -UseBasicParsing
```

### 5. 重启开发服务器

如果问题依然存在，尝试重启服务器：

1. 停止当前运行的服务（在运行的终端按 `Ctrl+C`）
2. 重新启动：
   ```bash
   # 终端1 - 后端
   cd backend
   python run.py
   
   # 终端2 - 前端
   npm run dev
   ```

### 6. 检查端口是否被占用

```powershell
# 检查端口占用
Get-NetTCPConnection -LocalPort 3000,8000 -ErrorAction SilentlyContinue
```

### 7. 查看Vite开发服务器日志

检查运行 `npm run dev` 的终端窗口，查看是否有编译错误或警告。

## 快速测试

创建一个简单的测试页面来验证 React 是否正常工作：

临时修改 `src/App.tsx` 为最简单的版本：

```tsx
import React from 'react'

const App: React.FC = () => {
  return <div style={{ padding: '20px' }}><h1>测试 - React正常</h1></div>
}

export default App
```

如果这个能显示，说明是组件的问题；如果还是白屏，说明是基础配置的问题。

## 常见解决方案

### 方案1：清除 node_modules 并重新安装
```bash
Remove-Item -Recurse -Force node_modules
Remove-Item package-lock.json
npm install
npm run dev
```

### 方案2：清除 Vite 缓存
```bash
Remove-Item -Recurse -Force node_modules/.vite
npm run dev
```

### 方案3：检查 TypeScript 配置
确保 `tsconfig.json` 配置正确，特别是 `jsx` 选项。

## 如果问题依然存在

请提供以下信息：
1. 浏览器控制台的具体错误信息（截图或复制文本）
2. Network 标签中的请求状态
3. 终端中的编译错误或警告
