# 启动开发服务器指南

## 第一步：安装依赖

### 安装后端依赖
```bash
pip install -r requirements.txt
```

### 安装前端依赖
```bash
npm install
```

## 第二步：启动服务

### 方式一：使用启动脚本（推荐）

**Windows:**
```bash
start.bat
```

**Linux/Mac:**
```bash
chmod +x start.sh
./start.sh
```

### 方式二：手动启动

**终端1 - 启动后端服务器:**
```bash
cd backend
python run.py
```
后端将在 http://localhost:8000 启动

**终端2 - 启动前端开发服务器:**
```bash
npm run dev
```
前端将在 http://localhost:3000 启动

## 验证服务是否启动

### 检查后端
- 访问 http://localhost:8000/docs 查看API文档
- 访问 http://localhost:8000/ 查看根路径

### 检查前端
- 访问 http://localhost:3000 查看前端界面

## 常见问题

### 端口被占用
如果8000或3000端口被占用，可以：
- 修改 `backend/run.py` 中的端口号
- 修改 `vite.config.ts` 中的端口号

### Python模块未找到
确保已安装所有依赖：
```bash
pip install -r requirements.txt
```

### Node模块未找到
确保已安装所有依赖：
```bash
npm install
```

## 使用流程

1. 打开浏览器访问 http://localhost:3000
2. 按照向导流程配置Agent并开始测试
3. 查看测试结果和报告

## API文档

后端服务启动后，可以访问：
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
